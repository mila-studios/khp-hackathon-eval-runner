## TODO

- [ ] Request VM with multiple CPU and enough disk space
- [ ] Get LLM keys
- [ ] Validate how many GPU we can use
- [ ] Validate `MAX_WORKERS`, `MAX_GPU` and `MAX_LLM_JUDGE` (currently 4, 1, 4)
- [ ] Find out how to send a job to a particular GPU
- [ ] Test saving model weights to S3
- [ ] Load model weights to S3
- [ ] Get list of team ids and team git repos
- [ ] Finalize `input.csv` data
- [ ] Finalize eval script (`eval/evaluate.py`)
- [ ] Predictions on CPU with a big model might require a lot of memory
